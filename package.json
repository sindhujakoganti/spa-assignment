{
  "name": "root",
  "private": true,
  "workspaces":["packages/client", "packages/server"],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "validate": "lerna run validate",
    "types:watch": "lerna run --parallel types:watch"
  },
  "dependencies": {
    "express-generator": "^4.16.1"
  }
}
